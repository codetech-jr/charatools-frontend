/**
 * @file app/catalogo/@modal/default.tsx
 * @description Slot vacío por defecto cuando no hay modal activo.
 * Requerido por Next.js para slots paralelos (Parallel Routes).
 */

export default function DefaultModal() {
  return null
}
